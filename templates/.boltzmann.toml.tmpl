# Generated by boltzmann at <$ now $>.
# Your oblique strategy of the day is:
#
# > <$ oblique $>
#
# via (https://npm.im/oblique-strategies)
version = "<$ version $>"

[args]
service = "<$ service $>"
<%- if postgres %>
postgres = "<$ postgres $>"
<% endif -%><%- if redis %>
redis = "<$ redis $>"
<% endif -%><%- if honeycomb %>
honeycomb = "<$ honeycomb $>"
<% endif -%>

[config]
ignore = [
  '.env-example',
  'middleware.*',
  'middleware/*',
  'handlers.*',
  'handlers/*',
  'body.*',
  'body/*',
  'package.json',
  '.gitignore',
  'README.md',
  <% if postgres %>'migrations/*.sql'<% endif %>
  # feel free to add ".boltzmann.toml" here if you've made edits.
]

