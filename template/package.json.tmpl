{
  "name": "idek",
  "description": "Increasing entropy with brains in boxes.",
  "version": "1.0.0",
  "author": "Ludwig Boltzmann",
  "dependencies": {
    "accepts": "^1.3.7",
    "are-we-dev": "^1.0.0",
    "culture-ships": "^1.0.0",
    "find-my-way": "^2.2.1",
    "bole": "^4.0.0",
    <% if honeycomb %>"honeycomb-beeline": "^1.8.3",<% endif %>
    <% if honeycomb %>"on-headers": "^1.0.2",<% endif %>
    <% if postgres %>"pg": "^7.12.1",<% endif %>
    <% if redis %>"handy-redis": "^1.6.2",<% endif %>
    <% if redis %>"redis": "^2.8.0",<% endif %>
    "ajv": "^6.10.2",
    "dotenv": "^8.1.0"
  },
  "devDependencies": {
    "@hapi/shot": "^4.1.2",
    "babel-eslint": "^10.0.3",
    "standard": "^14.3.1",
    "bistre": "^1.0.1",
    "tap": "^14.6.7"
  },
  "keywords": [],
  "license": "Apache-2.0",
  "main": "boltzmann.js",
  "private": true,
  "scripts": {
    "prestart": "docker-compose up -d",
    "start": "node -r dotenv/config boltzmann.js",
    "lint": "standard",
    "pretest": "docker-compose up -d",
    "test": "tap test",
    "test-cov": "tap --cov test",
    "posttest": "npm run lint"
  },
  "standard": {
    "parser": "babel-eslint",
    "ignore": [
      "boltzmann.js"
    ]
  }
}
